from distutils.core import setup
setup(
  name = 'experimentmanager',
  packages = [''], 
  version = '0.1',
  description = "Seattle's experimentmanager package",
  author = 'Seattle',
  url = 'https://github.com/BuildSeash/experimentmanager', # use the URL to the github repo
  classifiers = [],
)